(fun (revllcopy c) 
    (let ((newl nil))
        (loop (if (= c nil) (break newl)
            (block
                (set! newl (vec (vec-get c 0) newl))
                (set! c (vec-get c 1))
            )
        ))
    )
)

(fun (rangell s e) 
    (let ((i (- e 1)) (l nil))
        (loop (if (> i (- s 1))
            (block
                (set! l (vec i l))
                (set! i (- i 1))
            )
        (break l)))
    )
)

(let ((i 0) (l (rangell -5 20)))
    (loop (if (< i input)
        (block
            (set! l (revllcopy l))
            (set! i (+ i 1))
        )
    (break l)))
)

